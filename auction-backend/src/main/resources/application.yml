spring:
  application:
    name: auction-backend
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.222.128:3306/auction_db?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: zkzj102426@123
  redis:
    host: 192.168.222.128
    port: 16379
    password: zkzj102426
    timeout: 3000ms

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

server:
  port: 8080

admin:
  bootstrap:
    # 启动时确保存在一个可登录的管理员账号（不依赖id=1）。
    # 你可以按需改成自己的账号密码。
    username: admin
    password: admin123456
    # 若用户名已存在，是否重置其密码为上述 password
    reset-password: true
